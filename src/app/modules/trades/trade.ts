//create class Trade with variable tradeId, transactionType, buyer, seller, amount , currency, tradeDate, status
export class Trade {
    tradeId!: number;
    transaction!: string;
    buyer!: string;
    seller!: string;
    amount!: string;
    currency!: string;
    tradeDate!: string;
    status!: string;
}